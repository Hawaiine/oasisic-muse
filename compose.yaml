services:
  oasisic-muse:
    build: .
    image: barryallen26/oasisic-muse:latest
    container_name: oasisic-muse
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./data:/data
    environment:
      - TZ=Asia/Shanghai